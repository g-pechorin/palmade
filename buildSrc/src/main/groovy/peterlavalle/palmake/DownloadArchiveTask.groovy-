package peterlavalle.palmake

import org.gradle.api.tasks.TaskAction

class DownloadArchiveTask extends DefaultTask {

	String url

	DownloadArchiveTask(String url) {
		this.url = url
	}

	def path = url.substring(url.lastIndexOf('/') + 1)

	def cache = file('.cache/' + path)
	def dump = file('build/' + path)

	@TaskAction
	def download() {
		println 'hello from GreetingTask'
	}
}
